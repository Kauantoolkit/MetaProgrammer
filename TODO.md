- Fix the main class file name to match the class name by removing "generated" from the className if projectName contains "generated_"
- Ensure the pom.xml is generated by correcting the rootDir in generators
- Update rootDir in MainClassGenerator, PomGenerator, BaseStructureGenerator, GitignoreGenerator, ApplicationPropertiesGenerator, CodeGeneratorService, ThymeleafFrontGenerator, BackofficeControllerGenerator to Paths.get(System.getProperty("user.dir"))
- Test the generation
